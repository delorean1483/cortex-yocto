# fw_printenv / fw_setenv config for EcoFleet imx8mm-var-dart
# u-boot env lives at offset 0x400000 on /dev/mmcblk2 (eMMC boot media).
# Env size 128KB, raw (no filesystem).
#
# Device        Offset      Env.Size  Sector-size  Num-sectors
/dev/mmcblk2    0x400000    0x20000   0x200        0
